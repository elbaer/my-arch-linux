---
- name: clone yay
  git:
    repo: https://aur.archlinux.org/yay.git
    dest: /tmp/yay

- name: build yay
  shell: |
    cd /tmp/git/yay
    makepkg -si --noconfirm

- name: remove build directory
  file:
    path: /tmp/yay
    state: absent