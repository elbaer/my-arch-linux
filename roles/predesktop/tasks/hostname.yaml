---
- name: set hostname
  copy:
    src: hostname
    dest: /etc/hostname
    owner: root
    group: root
    mode: "u=rw,g=r,o=r"

- name: set hosts file
  copy:
    src: hosts
    dest: /etc/hosts
    owner: root
    group: root
    mode: "u=rw,g=r,o=r"